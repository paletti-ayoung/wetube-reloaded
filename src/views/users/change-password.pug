extends ../base

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="Old Password", name="oldPassword")
        input(placeholder="New Password", name="newPassword")
        input(placeholder="New Password Confirmation", name="newPasswordCon")
        input(type="submit" value="Change Password")